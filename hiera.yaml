---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: 'YAML backend'
    data_hash: yaml_data
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - 'common.yaml'
  
  - name: 'CAMELS backend'
    data_hash: yaml_data
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - 'common.yaml'
